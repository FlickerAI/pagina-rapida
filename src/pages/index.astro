---
import Layout from '../layouts/Layout.astro';
---

<Layout>
	<main>
		<!-- Navigation -->
		<nav class="nav animate-fade-in">
			<div class="logo">SUPER<span>TV</span></div>
			<div class="nav-links">
				<a href="#features">Funciones</a>
				<a href="#pricing">Precio</a>
				<button class="btn-primary">Empieza Ahora</button>
			</div>
		</nav>

		<!-- Hero Section -->
		<section class="hero content-wrapper">
			<div class="hero-text animate-fade-in">
				<span class="badge">El Futuro del Entretenimiento</span>
				<h1>Todo el contenido mundial, en un solo lugar.</h1>
				<p>Accede a más de 10,000 canales en vivo, miles de películas y series en 4K UHD. Sin contratos, sin complicaciones, solo diversión.</p>
				<div class="hero-cta">
					<button class="btn-primary lg">Obtener Super TV Box</button>
					<button class="btn-secondary">Ver Catálogo</button>
				</div>
			</div>
			<div class="hero-image-container animate-fade-in" style="animation-delay: 0.2s">
				<div class="hero-image-wrapper glass">
					<img src="/super_tv_hero_box.png" alt="Super TV Box 4K" class="hero-img" />
					<div class="image-glow"></div>
				</div>
			</div>
		</section>

		<!-- Features -->
		<section id="features" class="features content-wrapper">
			<h2 class="section-title">¿Por qué Super TV?</h2>
			<div class="features-grid">
				<div class="feature-card glass animate-fade-in">
					<div class="icon">📺</div>
					<h3>10,000+ Canales</h3>
					<p>Desde deportes en vivo hasta noticias internacionales y entretenimiento premium.</p>
				</div>
				<div class="feature-card glass animate-fade-in" style="animation-delay: 0.1s">
					<div class="icon">🚀</div>
					<h3>Plug & Play</h3>
					<p>Conecta el Super TV Box a tu televisor y empieza a ver en menos de 5 minutos.</p>
				</div>
				<div class="feature-card glass animate-fade-in" style="animation-delay: 0.2s">
					<div class="icon">💎</div>
					<h3>Calidad 4K UHD</h3>
					<p>Disfruta de tus programas favoritos con la máxima resolución y fluidez disponible.</p>
				</div>
			</div>
		</section>

		<!-- Pricing / Accessibility -->
		<section id="pricing" class="pricing content-wrapper">
			<div class="pricing-card glass animate-fade-in">
				<span class="badge">Oferta Limitada</span>
				<h2>Hazlo Tuyo Hoy</h2>
				<p class="price">$1,499 <span>MXN</span></p>
				<p>Pago único por el dispositivo. Acceso de por vida incluido.</p>
				<ul class="pricing-features">
					<li>✅ Super TV Box 4K Incluido</li>
					<li>✅ Control Remoto por Voz</li>
					<li>✅ Envío Gratis a todo el país</li>
					<li>✅ Soporte 24/7 en Español</li>
				</ul>
				<button class="btn-primary xl">Lo quiero ahora</button>
			</div>
		</section>

		<footer class="footer content-wrapper">
			<p>&copy; 2024 Super TV. Todos los derechos reservados.</p>
		</footer>
	</main>
</Layout>

<style>
	main {
		width: 100%;
		max-width: 1400px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	.content-wrapper {
		padding: 6rem 0;
	}

	/* Navigation */
	.nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 2rem 0;
	}

	.logo {
		font-size: 1.5rem;
		font-weight: 800;
		letter-spacing: -1px;
	}

	.logo span {
		color: var(--accent);
	}

	.nav-links {
		display: flex;
		align-items: center;
		gap: 2rem;
	}

	.nav-links a {
		color: var(--text-muted);
		text-decoration: none;
		font-weight: 500;
		transition: color 0.3s;
	}

	.nav-links a:hover {
		color: var(--text-main);
	}

	/* Buttons */
	.btn-primary {
		background: var(--accent);
		color: white;
		border: none;
		padding: 0.75rem 1.5rem;
		border-radius: 12px;
		font-weight: 600;
		cursor: pointer;
		transition: transform 0.2s, box-shadow 0.2s;
		box-shadow: 0 4px 20px var(--accent-glow);
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 30px var(--accent-glow);
	}

	.btn-primary.lg { padding: 1rem 2rem; font-size: 1.1rem; }
	.btn-primary.xl { width: 100%; padding: 1.25rem; font-size: 1.2rem; margin-top: 2rem; }

	.btn-secondary {
		background: rgba(255, 255, 255, 0.05);
		color: white;
		border: 1px solid var(--border);
		padding: 1rem 2rem;
		border-radius: 12px;
		font-weight: 600;
		cursor: pointer;
		transition: background 0.3s;
	}

	.btn-secondary:hover {
		background: rgba(255, 255, 255, 0.1);
	}

	/* Hero Section */
	.hero {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		min-height: 80vh;
	}

	.hero-text h1 {
		font-size: 4.5rem;
		line-height: 1.1;
		margin: 1.5rem 0;
		background: linear-gradient(to right, #fff, #94a3b8);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.hero-text p {
		font-size: 1.25rem;
		margin-bottom: 2.5rem;
	}

	.badge {
		background: rgba(59, 130, 246, 0.1);
		color: var(--accent);
		padding: 0.5rem 1rem;
		border-radius: 100px;
		font-size: 0.9rem;
		font-weight: 600;
		border: 1px solid rgba(59, 130, 246, 0.2);
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
	}

	.hero-image-container {
		position: relative;
	}

	.hero-image-wrapper {
		padding: 1rem;
		overflow: hidden;
		transform: perspective(1000px) rotateY(-5deg);
		transition: transform 0.5s;
	}

	.hero-image-wrapper:hover {
		transform: perspective(1000px) rotateY(0deg);
	}

	.hero-img {
		width: 100%;
		border-radius: 16px;
		display: block;
	}

	.image-glow {
		position: absolute;
		top: 50%;
		left: 50%;
		width: 120%;
		height: 120%;
		background: radial-gradient(circle, var(--accent-glow), transparent 70%);
		transform: translate(-50%, -50%);
		z-index: -1;
		opacity: 0.5;
	}

	/* Features */
	.section-title {
		text-align: center;
		font-size: 3rem;
		margin-bottom: 4rem;
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}

	.feature-card {
		padding: 3rem;
		text-align: center;
	}

	.feature-card .icon {
		font-size: 3rem;
		margin-bottom: 1.5rem;
	}

	.feature-card h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
	}

	/* Pricing */
	.pricing {
		display: flex;
		justify-content: center;
	}

	.pricing-card {
		max-width: 500px;
		text-align: center;
		padding: 4rem;
	}

	.price {
		font-size: 4rem;
		font-weight: 800;
		margin: 2rem 0 0.5rem;
		color: white;
	}

	.price span {
		font-size: 1rem;
		color: var(--text-muted);
	}

	.pricing-features {
		list-style: none;
		text-align: left;
		margin: 2rem 0;
	}

	.pricing-features li {
		padding: 0.75rem 0;
		border-bottom: 1px solid var(--border);
		color: var(--text-muted);
	}

	/* Footer */
	.footer {
		text-align: center;
		border-top: 1px solid var(--border);
		color: var(--text-muted);
	}

	/* Responsive */
	@media (max-width: 1024px) {
		.hero { grid-template-columns: 1fr; text-align: center; }
		.hero-text h1 { font-size: 3.5rem; }
		.hero-cta { justify-content: center; }
		.features-grid { grid-template-columns: 1fr; }
		.hero-image-wrapper { transform: none; }
	}
</style>
