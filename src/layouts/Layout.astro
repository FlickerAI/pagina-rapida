<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Super TV | Miles de Canales, Un Solo Box</title>
		<meta name="description" content="Experimenta el futuro del entretenimiento con Super TV. Cientos de canales, calidad 4K y la mejor accesibilidad del mercado." />
		
		<!-- Google Fonts: Outfit -->
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
	</head>
	<body>
		<div class="glow-container">
			<div class="glow-orb" id="orb1"></div>
			<div class="glow-orb" id="orb2"></div>
		</div>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--bg-dark: #050505;
		--accent: #3b82f6;
		--accent-glow: rgba(59, 130, 246, 0.5);
		--text-main: #ffffff;
		--text-muted: #94a3b8;
		--card-bg: rgba(255, 255, 255, 0.03);
		--border: rgba(255, 255, 255, 0.1);
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	html,
	body {
		font-family: 'Outfit', sans-serif;
		background-color: var(--bg-dark);
		color: var(--text-main);
		overflow-x: hidden;
		scroll-behavior: smooth;
	}

	body {
		min-height: 100vh;
	}

	/* Rich Background Effects */
	.glow-container {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		overflow: hidden;
		pointer-events: none;
	}

	.glow-orb {
		position: absolute;
		width: 600px;
		height: 600px;
		border-radius: 50%;
		filter: blur(120px);
		opacity: 0.15;
	}

	#orb1 {
		top: -200px;
		right: -100px;
		background: radial-gradient(circle, var(--accent), transparent);
	}

	#orb2 {
		bottom: -200px;
		left: -100px;
		background: radial-gradient(circle, #8b5cf6, transparent);
	}

	h1, h2, h3 {
		font-weight: 800;
		letter-spacing: -0.02em;
	}

	p {
		color: var(--text-muted);
		line-height: 1.6;
	}

	.glass {
		background: var(--card-bg);
		backdrop-filter: blur(12px);
		border: 1px solid var(--border);
		border-radius: 24px;
	}

	/* Micro-animations */
	@keyframes fadeIn {
		from { opacity: 0; transform: translateY(20px); }
		to { opacity: 1; transform: translateY(0); }
	}

	.animate-fade-in {
		animation: fadeIn 0.8s ease-out forwards;
	}

	/* Selection color */
	::selection {
		background: var(--accent);
		color: white;
	}
</style>

